// Общие константы, которые должны быть одинаковыми во всех расчётах себестоимости/остатков
// (warehouse.js, costEngine.js, stats.js) — раньше дата отсечки была захардкожена по отдельности
// в разных файлах, и это могло привести к тому, что они бы разъехались.

// Партии на "Поставках" введены как снимок остатков НА эту дату. Заказы до неё не должны
// списывать эти остатки повторно (см. costEngine.js — для таких заказов себестоимость
// оценивается по цене самой первой поставки товара, а не списанием с этих партий).
const STOCK_CUTOFF_DATE = '2026-06-01';

module.exports = { STOCK_CUTOFF_DATE };
