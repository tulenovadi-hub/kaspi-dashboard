node_modules/
dist/
.env
.env.*
!.env.example
.DS_Store
npm-debug.log*

.claude/settings.local.json

# Локальный npm install перед сборкой каждый раз создаёт этот файл заново, а в этом проекте
# его не хранят — один раз он уже попадал в репозиторий по невнимательности и его удаляли.
frontend/package-lock.json

# Временный стенд для проверки вёрстки на собранных стилях — создаётся под задачу и живёт
# только локально, в репозиторий попадать не должен (один раз уже попал по невнимательности).
frontend/harness.html
frontend/src/__harness.jsx
